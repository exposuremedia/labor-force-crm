import type { ReactNode } from "react";

export const metadata = {
  title: "Labor Force Link",
};

export default function EmbedLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
